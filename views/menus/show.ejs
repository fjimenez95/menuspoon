<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Menu</title>
</head>
<body>
    <h1>Restaurant Menu</h1>
    <h3>Name: <%= restaurant.name %></h3>
    <h3>Appetizers</h3>
    <% menu.appetizers.forEach(item => { %>
    <div><%= item.name %></div>
    <% }) %>
    <h3>Soups</h3>
    <% menu.soups.forEach(item => { %>
    <div><%= item.name %></div>
    <% }) %>
    <h3>Entrees</h3>
    <% menu.entrees.forEach(item => { %>
    <div><%= item.name %></div>
    <% }) %>

<!-- TODO: ADD NEW ITEM ON MENU -->
    <!-- <a href="/restaurant/new">New Item</a> -->
    <a href="/">Go Back</a>

</body>
</html>